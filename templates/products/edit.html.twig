{% extends 'base.html.twig' %}

{% block title %}{{'produits.edit'|trans}}{% endblock %}

{% block body %}
    <h1>{{'produits.edit'|trans}}</h1>

    {{ include('products/_form.html.twig', {'button_label': 'bouton.update'|trans}) }}

    <a href="{{ path('app_products_index') }}" class="btn btn-outline-info">{{'bouton.back'|trans}}</a>
{% endblock %}
